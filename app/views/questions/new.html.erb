<h1>Ask a Question already</h1>
<div class="row">
    <div class="col-md-4">
        <p>Guidelines</p>
        <ul>
            <li>Don't get too curious.</li>
            <li>If we say "Not Now Murray. Not NOW Murray. NOT. NOW. MURRAY!" don't take offense. It simply means the Belluminati has hacked into Bloccit and you can never be too safe. </li>
        </ul>
    </div>
    <div class="col-md-8">
        <%= form_for @question do |f| %>
            <div class="form-group">
                <%= f.label :title %>
                <%= f.text_field :title, class: "form-control", placeholder: "Enter a title for this question" %>
            </div>
            <div class="form-group">
                <%= f.label :body %>
                <%= f.text_area :body, rows:8, class: "form-control", placeholder: "Enter a gist of your question" %>
            </div>
            <div class="form-group">
                <%= f.submit "Save", class: "btn btn-success" %>
            </div>
        <% end %>
    </div>